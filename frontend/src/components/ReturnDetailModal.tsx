import { money } from '../utils/format'; import { printReturn } from '../utils/print';
export default function ReturnDetailModal({data,onClose}:{data:any;onClose:()=>void}){const h=data.header;return <div className="modal-mask"><div className="modal order-detail-modal"><div className="modal-head"><div><h2>退貨單 {h.ReturnNo}</h2><small>{h.ReturnDate} · 原銷貨單 {h.OrderNo}</small></div><div className="modal-head-actions"><button className="print-btn" onClick={()=>printReturn(data)}>列印</button><button onClick={onClose}>×</button></div></div><div className="order-detail-head"><div><span>客戶</span><b>{h.CustomerName}</b></div><div><span>狀態</span><b>{h.Status}</b></div><div><span>退貨總額</span><b>NT$ {money(h.TotalAmount)}</b></div></div><div className="order-lines-wrap"><table className="order-lines"><thead><tr><th>#</th><th>SKU</th><th>商品</th><th>退貨量</th><th>單價</th><th>小計</th></tr></thead><tbody>{data.items.map((x:any,i:number)=><tr key={x.Id}><td>{i+1}</td><td>{x.Sku}</td><td>{x.ProductName}</td><td>{x.Qty} {x.Unit}</td><td className="money">{money(x.UnitPrice)}</td><td className="money">{money(x.Amount)}</td></tr>)}</tbody></table></div>{h.Reason&&<div className="order-detail-note"><span>退貨原因</span><p>{h.Reason}</p></div>}</div></div>}
